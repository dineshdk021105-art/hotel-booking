export default function AdminSettings() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-6" style={{ color: 'var(--text-primary)' }}>Settings</h1>
      <div className="card-base p-8 text-center">
        <p style={{ color: 'var(--text-muted)' }}>System settings — configure hotel info, payment keys, notification settings and policies.</p>
      </div>
    </div>
  )
}
