export default function AdminFood() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-6" style={{ color: 'var(--text-primary)' }}>Food Menu Management</h1>
      <div className="card-base p-8 text-center">
        <p style={{ color: 'var(--text-muted)' }}>Food menu management — add categories and menu items after connecting Supabase.</p>
      </div>
    </div>
  )
}
