export default function AdminReports() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-6" style={{ color: 'var(--text-primary)' }}>Reports & Analytics</h1>
      <div className="card-base p-8 text-center">
        <p style={{ color: 'var(--text-muted)' }}>Revenue, occupancy and booking reports — connect Supabase for live data.</p>
      </div>
    </div>
  )
}
