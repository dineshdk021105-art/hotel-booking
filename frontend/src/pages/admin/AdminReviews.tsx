export default function AdminReviews() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-6" style={{ color: 'var(--text-primary)' }}>Reviews & Testimonials</h1>
      <div className="card-base p-8 text-center">
        <p style={{ color: 'var(--text-muted)' }}>Review moderation — approve or reject guest reviews and testimonials.</p>
      </div>
    </div>
  )
}
